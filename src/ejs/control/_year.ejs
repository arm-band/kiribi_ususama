<%
copyRightYear = "";
var getFullYear = new Date().getFullYear();
if(parseInt(getFullYear) - parseInt(commons.commons.year) === 0) {
   copyRightYear = commons.commons.year;
}
else {
   copyRightYear = `${commons.commons.year}-${getFullYear}`;
}
%>