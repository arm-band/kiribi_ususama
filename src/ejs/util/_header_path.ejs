<%
//ヘッダのパスをトップページならばアンカーのみ、そうでなければトップページの該当アンカー、というように振り分ける
funcHeaderPath = (variables, id) => {
    var headerPath = "";
    if(id !== "index") {
        headerPath = variables.commons.baseurl;
    }
    return headerPath;
}
%>