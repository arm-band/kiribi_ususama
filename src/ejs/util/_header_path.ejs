<%
//headタグ内のmeta属性のうち、titleとdescriptionとomgimageを判別
funcHeaderPath = (variables, id) => {
    var headerPath = "";
    if(id !== "index") {
        headerPath = variables.commons.baseurl;
    }
    return headerPath;
}
%>